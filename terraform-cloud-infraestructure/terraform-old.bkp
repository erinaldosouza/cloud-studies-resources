terraform {

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.16"
    }
  }

  cloud {
    organization = "cloud-computing-studies"
    workspaces {
      name = "aws-studies"
    }
  }

  required_version = ">= 1.2.0"
}
